defaults:
  - agent: rnd_mountain_car
  - _self_

env: MountainCarContinuous-v0

max_episode_steps: 400
num_eval_episodes: 3

device: cpu

seed: 1

hydra:
  job:
    name: ${env}
  run:
    dir: ./exp
