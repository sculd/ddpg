defaults:
  - agent: sac_fork_mountain_car
  - _self_

env: MountainCarContinuous-v0

max_episode_steps: 2000
num_eval_episodes: 3

device: cuda

seed: 1

hydra:
  job:
    name: ${env}
  run:
    dir: ./exp


